dist: xenial
language: python
python: 3.7
before_install:
  - pip install pytest pytest-cov
  - pip install coveralls
  - pip install pandas
install:
  - python setup.py develop
script:
  - pytest --cov=mahjong
after_success:
  - coveralls
